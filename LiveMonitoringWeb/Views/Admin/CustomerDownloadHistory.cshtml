@model IEnumerable<LiveMonitoringWeb.Models.CustomerDownloadHistory>

@{
    String AppBaseURLWithSlash = new CommonUtility.AppSettingReader().ReadKey("WebBaseURL");
    String AppBaseURL = AppBaseURLWithSlash.Substring(0, AppBaseURLWithSlash.Length - 1);
    ViewBag.Title = "CustomerDownloadHistory";
    Layout = "~/Views/Shared/_Layout_AdminOuth.cshtml";
}


<script type="text/javascript">
    $(document).ready(function () {
        $('#table').dataTable({
            "iDisplayLength": 10,
            "bDestroy": true
        });
    });
</script>

<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h3><span class="glyphicon glyphicon-list-alt"></span> Customer Download History</h3>
            <hr />
        </div>
        <div class="col-sm-12 table-responsive ">
            <table id="table" style=" border-right: 1px solid #000000; " class="CSSTableGenerator">
                <thead>
                    <tr style=" background-color: #4C638E; color: white; height: 30px;">
                        <th>
                            @Html.Label("Name")
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.customer.OrganizationName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.customer.Email)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.DownloadDate)
                        </th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td style=" border-left: 1px solid #ddd; border-top: 1px solid #ddd;">
                                @Html.DisplayFor(modelItem => item.customer.FirstName) &nbsp; @Html.DisplayFor(modelItem => item.customer.LastName)
                            </td>
                            <td style=" border-left: 1px solid #ddd; border-top: 1px solid #ddd;">
                                @Html.DisplayFor(modelItem => item.customer.OrganizationName)
                            </td>
                            <td style=" border-left: 1px solid #ddd; border-top: 1px solid #ddd;">
                                @Html.DisplayFor(modelItem => item.customer.Email)
                            </td>
                            <td style=" border-left: 1px solid #ddd; border-top: 1px solid #ddd;">
                                @Html.DisplayFor(modelItem => item.DownloadDate)
                            </td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <br />
    <br />
    <br />
    <br />
</div>

